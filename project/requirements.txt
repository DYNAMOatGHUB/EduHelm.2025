# Core Django
Django>=3.1,<3.2
Pillow>=10.0.0

# MongoDB
djongo==1.3.7
pymongo==3.12.3  # Upgraded for better SSL/TLS support with MongoDB Atlas
sqlparse==0.2.4
dnspython==2.3.0  # Required for MongoDB Atlas (mongodb+srv:// URIs)
certifi  # SSL certificate bundle for secure connections

# Production Server
gunicorn==21.2.0

# Static Files
whitenoise==6.6.0

# Environment Variables
python-decouple==3.8

# Database URL parsing (removed - conflicts with djongo/Django 3.1)
# dj-database-url is optional and requires Django>=3.2. Removed to keep djongo compatibility.
